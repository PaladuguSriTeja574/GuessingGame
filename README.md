# GuessingGame
A basic project build using python language and library called Tkinter where we should guess the numbers based on their positions(bull represents a number in correct place and cow represents a number in wrong place) finally we should guess the number in taken amount of tries.
